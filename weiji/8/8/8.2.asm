A8255_CON EQU 0606H
A8255_A EQU 0600H
A8255_B EQU 0602H
A8255_C EQU 0604H

DATA SEGMENT
TABLE1:
    DB 7DH
    DB 6DH
    DB 66H
    DB 4FH
    DB 5BH
    DB 06H
DATA ENDS
CODE SEGMENT
    ASSUME CS:CODE,DS:DATA
START:
    MOV AX,DATA
    MOV DS,AX
    
    MOV DX,A8255_CON ;INIT
    MOV AL,80H
    OUT DX,AL

X1:    
    LEA SI,TABLE1
    MOV CX,06H
    MOV AL,11011111B
X2: 
    MOV DX,A8255_A
    OUT DX,AL
    ROR AL,1
    PUSH AX
    MOV AL,[SI]
    MOV DX,A8255_B
    OUT DX,AL
    POP AX
    CALL DELAY
    CALL DELAY
    CALL DELAY
    CALL DELAY
    CALL DELAY
    CALL DELAY
    CALL DELAY
    CALL DELAY

    CALL DELAY
    INC SI
    LOOP X2
    JMP X1    
DELAY:
    PUSH CX
    MOV CX,0FFFFH
X3:
    LOOP X3
    POP CX
    RET
CODE ENDS
     END START